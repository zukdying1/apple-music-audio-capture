<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 11+ package visibility: host process can resolve module components. -->
    <queries>
        <package android:name="com.neurax08.xposed.appledecryptor" />
        <package android:name="com.apple.android.music" />
        <intent>
            <action android:name="com.neurax08.xposed.appledecryptor.action.QUEUE_UPSERT" />
        </intent>
        <provider android:authorities="com.neurax08.xposed.appledecryptor.queue" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeEmptyActivity">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ComposeEmptyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".download.DownloadNotificationService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />
        <!-- Cross-process queue: Apple Music hook writes here; module UI reads. -->
        <provider
            android:name=".download.QueueContentProvider"
            android:authorities="com.neurax08.xposed.appledecryptor.queue"
            android:exported="true"
            android:grantUriPermissions="true" />
        <!-- Explicit broadcast target for host → module queue sync (Android 11+ safe). -->
        <receiver
            android:name=".download.QueueSyncReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.neurax08.xposed.appledecryptor.action.QUEUE_UPSERT" />
                <action android:name="com.neurax08.xposed.appledecryptor.action.QUEUE_DELETE" />
                <action android:name="com.neurax08.xposed.appledecryptor.action.QUEUE_PING" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
